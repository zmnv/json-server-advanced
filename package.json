{
  "name": "json-server-advanced",
  "version": "1.0.0",
  "description": "REST API server based on multiple .JSON files",
  "main": "app.js",
  "scripts": {
    "start": "node app.js",
    "dev": "npm run lint && nodemon app.js",
    "lint": "./node_modules/.bin/eslint '**/*.js'"
  },
  "author": "Valeriy Zimnev",
  "license": "MIT",
  "devDependencies": {
    "eslint": "^5.10.0",
    "json-server": "^0.14.0"
  },
  "dependencies": {
    "jmespath": "^0.15.0"
  },
  "nodemonConfig": {
    "ignore": [
      "routes.json"
    ]
  }
}
